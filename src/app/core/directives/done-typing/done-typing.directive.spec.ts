import { DoneTypingDirective } from './done-typing.directive';

describe('DoneTypingDirective', () => {
  it('should create an instance', () => {
    const directive = new DoneTypingDirective();
    expect(directive).toBeTruthy();
  });
});
